=========
Settings
=========

.. py:attribute::frequency

    MonkTrader only provide two options for `frequency`--`tick` and `1m`.
    `tick` means the strategy would run in the tick level. Every tick would
    trigger your strategy method.

Core Settings
=============
